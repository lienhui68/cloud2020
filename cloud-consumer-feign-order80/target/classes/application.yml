server:
  port: 80

spring:
  application:
    name: cloud-consumer-consul-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}